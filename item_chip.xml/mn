<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 기존 권한 유지 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 새로 추가 -->
    <!-- 사진 선택/카메라 사용 권한 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Android 13 이상 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MakeSalfApp"
        tools:targetApi="31">

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBdX_FSgBWcpzOqD64PxVFaJD2yM0Nzi08" />

        <!-- Kakao SDK key -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="akdfadfladflka" />

        <!-- Activity 선언 (필요한 것들) -->
        <activity android:name=".MapsActivity" android:exported="false" android:label="@string/title_activity_maps" />
        <activity android:name=".ReviewActivity" android:exported="true"/>
        <activity android:name=".StoreRegistrationActivity" android:exported="true"/>
        <activity android:name=".MenuManagementActivity" android:exported="false" android:label="메뉴 관리 화면" />
        <activity android:name=".NewsManagementActivity" android:exported="false" android:label="소식 관리 화면" />
        <activity android:name=".Activityrestaurant" android:exported="true"/>
        <activity android:name=".signup1Activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".googleMap" />
        <activity android:name=".MyInfoActivity"/>
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase 초기화 필요시 meta-data 추가 가능 -->
        <!-- 예: Firebase Messaging -->
        <!--
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        -->
    </application>

</manifest>
